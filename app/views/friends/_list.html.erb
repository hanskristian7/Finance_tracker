<div class="row">
    <div class="col-9">
        <table class="table table-hover mt-3">
            <thead>
            <tr>
                <th scope="col">Email</th>
                <th scope="col">First Name</th>
                <th scope="col">Last Name</th>
                <th scope="col" colspan="2">Action</th>
            </tr>
            </thead>
            <tbody>
                <% @friends.each do |friend|  %>
                <tr>
                    <td><%= friend.email %></td>
                    <td><%= friend.first_name %></td>
                    <td><%= friend.last_name %></td>
                    <td> <%= link_to 'Profile', user_path(friend.id), class: "btn btn-warning" %> </td>
                    <td> <%= link_to 'Delete', friendship_path(friend), method: :DELETE, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %> </td>
                </tr>
                <% end %>
            </tbody>
        </table>
    </div>
</div>